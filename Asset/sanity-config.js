// Configurazione di default: sostituisci <PROJECT_ID> e, se serve, il dataset.
// Puoi anche definire window.SANITY_CONFIG in linea sulle pagine se preferisci.
window.SANITY_CONFIG = window.SANITY_CONFIG || {
  projectId: 'y5t2piic',
  dataset: 'production',
  apiVersion: '2024-01-01',
  useCdn: false,
}
